FROM python:3.11-slim

RUN apt-get update && \
    apt-get install -y git git-lfs && \
    pip install --no-cache-dir axolotl==0.6.5 bitsandbytes datasets accelerate && \
    rm -rf /var/lib/apt/lists/*

WORKDIR /workspace
ENTRYPOINT ["axolotl"]
